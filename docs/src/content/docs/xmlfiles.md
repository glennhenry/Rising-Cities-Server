---
title: XML Files
slug: xmlfiles
description: XML Files
---

Notes about XML Files.

### settings.xml

The `settings.xml` is server configuration that can override the game's default configuration. The settings listed there is considered as `DeveloperSettingsProxy`. One usage is overriding the server's host and port of `ServerVo`, which is primarily set by the JSON response of `RCApi`.
